# Work Content Replacement

The first three InfiniteMenu entries become evidence-led portfolio categories while the menu's WebGL motion, scale, and snapping remain unchanged. A monochrome nine-tap WebGL blur is applied inside each disc so every cover keeps the soft, long-exposure treatment while the source images remain sharp on detail pages. `Publication` uses the supplied GBT-BiLSTM workflow diagram and opens Changjun Li's Google Scholar profile. Its compact copy identifies the JCR Q1 publication, granular-ball computing, BiLSTM forecasting, and nine financial datasets.

`Research` uses the supplied head-mounted reconstruction hardware image. Its copy summarizes the resume's major research threads: fuzzy multi-scale rough sets, scientific image forensics, financial time-series forecasting, and head-mounted 3D reconstruction. Its action opens `/research` in a new tab centered on Erlang Tianyan and the broader research record.

The Research detail page presents the Erlang Tianyan overview first and the supplied Research Experience résumé extract second, both at their original aspect ratios. Direct visits to the homepage without a URL hash disable browser scroll restoration and reset to the hero; explicit navigation hashes continue to target their requested section.

`Projects` replaces Item 3, using the supplied AI systems overview as its cover and opening `/projects` in a new tab. The Projects route contains the AI-native education and JVM protection overview. Both detail routes preserve their supplied plate at its original aspect ratio without cropping or overlays, adding only accessible captions and a return link.

The active item's supporting copy uses a 28vw/30ch horizontal measure so it reads in a few short lines rather than a vertical word stack. Pale blue type and a dense black shadow preserve contrast when the text crosses a bright grayscale disc.
